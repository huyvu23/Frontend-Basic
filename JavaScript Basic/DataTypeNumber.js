// Làm việc với number
var result = 20 / "abc";

// Cách kiểm tra số NaN
// console.log(isNaN(result));

// Làm việc với Number
// - To String
// - To Fixed

var age = 18;
console.log(typeof age.toString());

// giá trị thập phân nhỏ hơn 5 thì làm tròn dưới, > 5 làm tròn trên
var PI = 3.14;
console.log(typeof PI.toFixed());
